# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies
node_modules

# Build outputs
dist
dist-ssr

# Environment files (never commit real secrets)
.env
.env.*
.env.local
.env.*.local

# Secrets / credentials
.venv-secrets/
*.pem
*-firebase-adminsdk-*.json
service-account*.json

# Firebase
.firebase/
functions/service-account.json
functions/*-firebase-adminsdk-*.json
functions/package-lock.json

# Vercel
.vercel

# Editor / IDE
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.DS_Store

# Personal / workspace notes (not part of the project)
ObsiddyTOO/
.claude/
.cursor/
.agents/

# OS / misc
Thumbs.db

# === Open source migration & internal artifacts ===
# These were useful during the OSS conversion but are not needed
# in the public repository going forward.
OPENSOURCE_CHECKLIST.md
OPENSOURCE_MIGRATION.md
OPEN_SOURCE_READY.txt

# Internal planning and competitor research
docs/TRVLTOO_ROAMY_COMPETITOR_PLAN.md
docs/df.md

# Secrets scanning baseline (generated locally)
.secrets.baseline

# AI/agent tooling lock files (not part of the application)
skills-lock.json

# AI/agent personal tooling directories (if not already covered above)
agent/
